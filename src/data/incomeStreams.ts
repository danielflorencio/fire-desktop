import { incomeStreamCategories } from "./incomeStreamCategories"
import { IncomeStream } from "../types/incomeStream"
// // import { Category } from '../types/category';
export const incomeStreams: IncomeStream = {
    websites: { name: "Websites", monthlyReturns: 0, timeSpent: 0 }
}
